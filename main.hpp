#ifndef MAIN_HPP
#define MAIN_HPP

#include <string>
#include <vector>

extern void log_data(std::string data);
extern std::vector<std::string> split(std::string str, const char delimiter);
std::string case_switch(std::string str);

#endif
